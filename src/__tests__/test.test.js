
/**
 * Note that with Jest, the describe and expect functions are globally available and thus require no import
 * https://jestjs.io/docs/en/api
 */
describe("basic tests", () => {
    it('shows up as a test', () => {
        expect(true).toEqual(true);
        
      });

})
